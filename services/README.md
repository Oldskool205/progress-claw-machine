# Services

Stores long-running services and always-on system processes.

## Subfolders

- `runtime/`: service runtime definitions
- `background/`: background workers and scheduled tasks
- `watchdog/`: health checks and automatic recovery
- `logging/`: service logging configuration and notes

Services are placeholders for future phases. No service runtime logic is implemented yet.
