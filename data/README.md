# Data

The data module stores generated and reference data used by Progress Claw OS.

## Subfolders

- `raw/`: unprocessed captures and source data
- `processed/`: cleaned or transformed data
- `models/`: AI model artifacts
- `logs/`: runtime logs

Large generated files should not be committed unless they are small test fixtures.
