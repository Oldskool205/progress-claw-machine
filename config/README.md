# Config

The config module contains project configuration files.

## Subfolders

- `environments/`: environment-specific settings
- `hardware/`: hardware profiles and device settings
- `interfaces/`: contracts between modules
- `secrets.example/`: safe examples for secret configuration

Use `config/config.yaml` as the default starter configuration.
