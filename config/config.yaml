project:
  name: Progress Claw OS
  version: 1.0.0
  phase: 4
  environment: development

controller:
  active_adapter: arduino
  safety_boundary: true

camera:
  enabled: false

arduino:
  enabled: true
  mock_when_disconnected: true
  baud_rate: 115200

dashboard:
  enabled: true

ai:
  enabled: false
