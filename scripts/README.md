# Scripts

The scripts module contains repeatable helper commands.

## Subfolders

- `dev/`: local development scripts
- `deploy/`: deployment helpers
- `maintenance/`: operator and maintenance scripts
- `tools/`: small project utilities

Scripts should be simple, documented, and safe to run repeatedly.
