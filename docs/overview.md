# Claw MC Overview

Claw MC is organized around a Paper-based Minecraft server with modular plugins.

## Goals

- Keep shared platform behavior in `claw-core`.
- Keep lobby behavior isolated in `claw-lobby`.
- Keep gameplay systems isolated in `claw-gameplay`.
- Store server configuration and content separately from Java source code.
